pipeline {
    agent any
    environment {
        DOCKER_IMAGE = 'docker.io/ahmedmaher07/ivolve'
        KUBE_NAMESPACE = 'default'
        DEPLOYMENT_YAML = 'deployment.yaml'
    }
    stages {
        stage('Checkout') {
            steps {
                script {
                    git url: 'https://github.com/IbrahimAdell/Lab.git', branch: 'main'
                    dir('jenkins/lab1') {
                        git url: 'https://github.com/wolf8534/ivolve-.git', branch: 'main'
                    }
                }
            }
        }

        stage('Deploy to Kubernetes') {
            steps {
                script {
                    // استخدام الـ Token كـ Credential للنشر
                    withCredentials([string(credentialsId: 'new_k8s_token')]) {
                        // استخدام token من الـ Jenkins credentials للنشر
                        sh """
                        kubectl --token=$new_k8s_token apply -f $DEPLOYMENT_YAML -n $KUBE_NAMESPACE
                        """
                    }
                }
            }
        }

        stage('Post Action') {
            steps {
                script {
                    echo "Deployment to Kubernetes completed successfully!"
                }
            }
        }
    }

    post {
        success {
            echo "Pipeline executed successfully."
        }
        failure {
            echo "Pipeline failed."
        }
    }
}
